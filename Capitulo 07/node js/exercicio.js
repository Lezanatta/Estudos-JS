const frase = "elefante";

const copia = frase.substr(2,3);

const primeiraOcorrencia = frase.indexOf("a");
const ultimaOcorrencia = frase.lastIndexOf("e");

console.log(copia);

console.log(primeiraOcorrencia);

console.log(ultimaOcorrencia);